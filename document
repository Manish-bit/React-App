watch  for bem model in css

:root{
    --color-black: #0000
}
 Now this can be used by any other css classes as

 color: var(--color-black)

 while applying media query
 @media screen and (max-width){
    {
        
    }
 }
 inset property in css


 problem from project before

 onChange={(e) => setInputValue(e.target.value)}

 remember this 